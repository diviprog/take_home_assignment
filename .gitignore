# Build output
/bin/
*.exe

# Discovery-pass local cache (rendered pages + raw API responses); the
# stitched items.json and tally.md in notes/ are the committed artifacts
/cache/

# Local env / secrets
.env
.env.local

# OS
.DS_Store
